interface:
  display_name: "Active Slice Watchlist"
  short_description: "Per-slice supervision watchlist for what agents must watch, verify, or reject."
  default_prompt: "Use $important-instruction-ledger as an active slice watchlist before planning, worker supervision, direct edits, closeout, or commits."
