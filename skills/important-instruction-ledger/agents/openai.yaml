interface:
  display_name: "Important Instruction Ledger"
  short_description: "Durable active constraints for agents."
  default_prompt: "Use $important-instruction-ledger to record and revisit important user instructions before planning, worker supervision, edits, closeout, or commits."
