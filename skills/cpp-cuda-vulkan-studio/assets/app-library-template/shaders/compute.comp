#version 450

layout(local_size_x = 64, local_size_y = 1, local_size_z = 1) in;

layout(set = 0, binding = 0, std430) buffer SmokeValues {
    uint values[];
} smoke;

void main() {
    if (gl_GlobalInvocationID.x == 0) {
        smoke.values[0] = 0xdecafbadu;
    }
}
