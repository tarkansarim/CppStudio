# Vulkan Lane

Vulkan runtime code, shader assets, SPIR-V tooling, device capability checks, and validation-layer
evidence.

## Canonical Docs

- `docs/DEVELOPMENT_ENVIRONMENT.md`
- `docs/VALIDATION_PIPELINE.md`

## Primary Paths

- `src/render/`
- `shaders/`

## Update When

- Vulkan instance/device setup, queues, descriptors, shader interfaces, synchronization, render or
  compute smoke tests, or validation-layer policy changes
- shader compilation or SPIR-V validation changes
- Vulkan portability requirements change
