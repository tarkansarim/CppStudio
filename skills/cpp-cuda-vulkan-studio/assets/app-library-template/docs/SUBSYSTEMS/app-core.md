# App Core

Application entrypoint, reusable library code, public headers, and project-owned runtime behavior.

## Canonical Docs

- `README.md`

## Primary Paths

- `src/app/`
- `src/core/`
- `include/`

## Update When

- app startup, command-line behavior, library API, public headers, or core data flow changes
- a new runtime subsystem becomes a first-class ownership lane
- tests or benchmarks depend on new app/core behavior
