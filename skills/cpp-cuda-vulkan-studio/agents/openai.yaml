interface:
  display_name: "C++ CUDA Vulkan Studio"
  short_description: "Native C++ GPU backbone, not frontend or data work"
  default_prompt: "Use $cpp-cuda-vulkan-studio to create or upgrade a Vulkan-first C++ GPU project; keep CUDA explicit."
