interface:
  display_name: "C++ CUDA Vulkan Studio"
  short_description: "Vulkan-first C++ backbone with optional CUDA lanes and donor references"
  default_prompt: "Create or upgrade a Vulkan-first C++ GPU project with explicit CUDA lanes only when needed."
